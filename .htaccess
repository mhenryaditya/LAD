<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /LAD

    # Hanya arahkan ke index.php jika bukan file atau direktori yang ada
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]

</IfModule>
